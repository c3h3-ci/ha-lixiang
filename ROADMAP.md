# 版本路线图

> 当前：**v0.9.0 (Beta)**
> 目标：**v1.0.0 (稳定版)**

---

## 为什么现在是 Beta

| 维度 | 现状 | 1.0 要求 |
|---|---|---|
| **车型覆盖** | 仅理想 L6（一辆） | ≥3 车型（L6 + L7/L9/MEGA 任一） |
| **账号类型** | 仅车主账号 | 车主 + 家人账号均验证 |
| **多车** | ❌ 未支持 | 支持（或多账号独立接入） |
| **单元测试** | ❌ 无 | 核心逻辑覆盖 |
| **信号语义** | ⚠️ 部分靠源码还原 | 长期观察确认 |
| **首次登录** | ⚠️ 仅验证 1 次 | 多环境（网络/浏览器）验证 |
| **错误处理** | ⚠️ 散落 | 统一策略 + 重连 + 告警 |

---

## 到 1.0 的待办

### P0 —— 阻塞 1.0

- [ ] **信号语义修正（task-14 发现）**
  - [ ] `charge_gun_ac`：改判据为 `== 2`（App: `XChargeDataHandle.smali:102`）
  - [ ] `ChargeStatus`：复刻 App 归一化函数 `{40,50,60,70,80,90,111}`
  - [ ] 尾门：实现 App 的「Lock 优先 → Switch fallback」聚合
  - [ ] `translations.py` 门/窗语义修正（8 处）
  - [ ] `tank_lock`：按 `KeyUnity` 归属确认（勿盲改）

- [ ] **补单元测试**
  - [ ] 抽出 `render()` 纯函数（替代 sensor.py 29 处 `if key ==`）
  - [ ] 参数化等价性测试（value × 全部 key）
  - [ ] 覆盖：门/窗/锁/空调/充电 语义

- [ ] **首次登录多环境验证**
  - [ ] 不同网络（内网/外网/移动）
  - [ ] 不同浏览器（Chrome/Safari/Firefox）
  - [ ] 滑动验证失败的容错提示

- [ ] **多车型验证**
  - [ ] L7 / L8 / L9 / MEGA / i 系列 各一辆
  - [ ] 记录各车型的功能差异

### P1 —— 提升质量

- [ ] **架构阶段 2**（见 `docs/集成架构优化方案_20260923.md`）
  - [ ] `SignalSpec` 声明式化 + `tools/gen_signals.py`
  - [ ] `freq` 驱动分频
  - [ ] `ResultPolicy` 收敛 401 重试
  - [ ] HTML 外置

- [ ] **错误处理**
  - [ ] 统一异常基类
  - [ ] 断线重连
  - [ ] 持续失败告警（HA persistent_notification）

- [ ] **信号新鲜度**
  - [ ] 用 `ts` 判断有效性（旧的标记 unavailable）
  - [ ] VSS 批次失败的降级策略

### P2 —— 锦上添花

- [ ] 数据流监控（可选）
- [ ] 更多实体（App 有但未做的 55 条信号）
- [ ] 仪表盘示例

---

## 已知问题（当前 Beta）

1. **部分信号语义待核实** —— `charge_gun_ac` / `ChargeStatus` / `tank_lock`
2. **尾门未实现聚合** —— 只读 Switch，未做 Lock 优先
3. **车控命令超时** —— pushState=1 超时后状态延迟一轮才更新
4. **多车未支持** —— 第二个账号接入同一辆车会被拒绝（VIN 去重）
5. **VSS 批次失败日志** —— 「批次(2)反复失败」偶现（不影响主数据）

---

## 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.9.0 | 2026-09-23 | 首个 Beta：106 实体、车控、定位、通知 |

---

## 反馈

提 [Issue](https://github.com/c3h3-ci/ha-lixiang/issues) 时请附：
- HA 版本 + 集成版本
- 「设备 → 下载诊断」导出的 JSON（已脱敏）
- 相关日志（设置 → 系统 → 日志，搜索 `lixiang_auto`）
