# 版本路线图

> 当前：**v0.11.0 (Beta)**
> 目标：**v1.0.0 (稳定版)**

---

## 进度速览

| 类别 | 完成 | 说明 |
|---|---|---|
| **P0 信号语义** | ✅ 100% | 尾门聚合、门二态、车窗、ChargeStatus 复刻 |
| **P0 单元测试** | ✅ 100% | 104 个测试（含等价性 + 防护）|
| **P0 首次登录验证** | ⏳ 0% | 需多网络/浏览器环境 |
| **P0 多车型验证** | ⏳ 0% | 需其他车主 |
| **P1 架构阶段 2** | ✅ 100% | 8 个子项全部完成 |
| **P1 错误处理** | ✅ 100% | 分级告警 + health 诊断 |
| **P1 信号新鲜度** | ✅ 100% | 数据年龄属性 + 未上报判定 |
| **P2 补充** | 🟡 30% | 已补 15 条信号，仪表盘未做 |

---

## 为什么现在是 Beta

| 维度 | 现状 | 1.0 要求 |
|---|---|---|
| **车型覆盖** | 仅理想 L6（一辆） | ≥3 车型 |
| **账号类型** | 仅车主账号 | 车主 + 家人账号 |
| **多车** | ❌ 未支持 | 支持 |
| **单元测试** | ✅ 104 个 | ✅ 已达标 |
| **信号语义** | ✅ 源码还原 + 实测 | ⏳ 长期观察 |
| **首次登录** | ⚠️ 仅验证 1 次 | 多环境验证 |
| **错误处理** | ✅ 分级 + 告警 | ✅ 已达标 |

---

## ✅ 已完成

### P0 —— 阻塞 1.0 的核心项

- [x] **信号语义修正**（依据 App 源码，非猜测）
  - [x] `charge_gun_ac`：`== 2` 判据（`XChargeDataHandle.smali:102`）
  - [x] `ChargeStatus`：复刻 `getChargeState()` 归一化函数
  - [x] 尾门：实现 Lock 优先聚合（`getTrunkState()`）
  - [x] `translations.py` 门/窗语义修正（二态 + 0-100 位置值）
  - [x] `tank_lock`：确认归属 `KeyUnity`（未盲改）
  - [x] 空调：`FOffStatus` 替代 `ExSpeedStatus`
  - [x] 充电口盖：用 `ChrgPorLidStsV2` + `-1` 哨兵

- [x] **补单元测试**（104 个）
  - [x] `rendering.py` 纯函数抽出（29 处 `if key ==` → 可测）
  - [x] 等价性测试（新旧表逐字段对比）
  - [x] 防护测试（函数完整性、Semantics 引用）
  - [x] 虚拟信号测试（online_status）

### P1 —— 质量提升

- [x] **架构阶段 2**（全部完成）
  - [x] 2.1/2.2 `SignalSpec` 声明式表 + 生成脚本
  - [x] 2.3 coordinator 消费 `spec.freq`
  - [x] 2.4 sensor 消费 `SignalSpec`
  - [x] 2.5 binary_sensor 用 `Semantics` 枚举
  - [x] 2.6 `ResultPolicy` 收拢 401 重试（3 处 → 1 处）
  - [x] 2.7 `_LOGIN_HTML` 外置（113 行 HTML → login_page.html）
  - [x] 2.8 `VSS_PATHS` 迁移（消除 169 行重复）

- [x] **错误处理**
  - [x] 分级：1-2 静默 / 3-4 warning / 5+ 通知
  - [x] HA 持久通知（含错误详情 + 建议）
  - [x] 恢复后自动清除
  - [x] `coordinator.health` 诊断属性

- [x] **信号新鲜度**
  - [x] `数据年龄` 属性（"2 小时前"）
  - [x] `ts == "0"` → unavailable（硬件不存在）
  - [x] 非 VSS 信号跳过判定（修复 online_status）

### 工程化

- [x] `li-verify.sh` —— 7 项强制验证
- [x] GitHub Actions CI（hassfest + HACS + lint）
- [x] pre-commit hook
- [x] `li-pr.sh` —— PR 工作流
- [x] `li-sync.sh` —— 双副本同步
- [x] `bump.sh` —— 版本管理
- [x] `VERIFY.md` + `CONTRIBUTING.md`

---

## ⏳ 待完成

### P0 —— 需要外部条件

- [ ] **首次登录多环境验证**
  - [ ] 不同网络（内网 / 外网 / 移动）
  - [ ] 不同浏览器（Chrome / Safari / Firefox）
  - [ ] 滑动验证失败的容错提示
  - **阻塞原因**：需要多个测试环境

- [ ] **多车型验证**
  - [ ] L7 / L8 / L9 / MEGA / i 系列
  - [ ] 记录各车型功能差异
  - **阻塞原因**：需要其他车主配合

### P2 —— 锦上添花

- [ ] 数据流监控（信号变化历史图表）
- [ ] 补充剩余中低价值信号
- [ ] 仪表盘示例（Lovelace 卡片）
- [ ] 多语言（英文翻译）

---

## 架构改造收益

### 新增一个信号：改动点对比

```
【改造前】5 处
  ① const.py        VSS 路径
  ② sensor.py       _mk(...) 描述
  ③ coordinator.py  分频前缀（如需）
  ④ translations.py 值翻译（如需）
  ⑤ binary_sensor.py（如果是二元）

【改造后】1 处
  signals.py 加一行 SignalSpec(...)
```

### 代码量变化

| 项 | 变化 |
|---|---|
| `const.py` VSS_PATHS | −169 行（迁移到 signals）|
| `sensor.py` `_compute_value` | −200 行（抽到 rendering）|
| 测试 | +476 行（104 个测试）|

---

## 已知问题

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| 1 | 家人账号 `vehicleStatus` 返回 null | 在线状态靠 5G/XCU 兜底 | ✅ 已兜底 |
| 2 | 部分信号 L6 无数据 | 已移除（天窗/CLTC 里程）| ✅ 已处理 |
| 3 | 多车未支持 | 第二个 VIN 会被拒绝 | ⏳ 未做 |
| 4 | VSS 批次偶发失败 | 不影响主数据 | 🟡 观察中 |

---

## 版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| **v0.11.0** | 2026-09-24 | **座椅改 fan（档位 UI）+ 尾门/车窗改 cover + 登录修复 + 英文翻译** |
| v0.10.0 | 2026-09-24 | 优化添加集成流程（VIN 兜底/校验/引导）|
| v0.9.1 | 2026-09-23 | P0 信号语义修复 |
| v0.9.0 | 2026-09-23 | 首个 Beta |

---

## 反馈

提 [Issue](https://github.com/c3h3-ci/ha-lixiang/issues) 时请附：
- HA 版本 + 集成版本
- 「设备 → 下载诊断」导出的 JSON（已脱敏）
- 相关日志（设置 → 系统 → 日志，搜索 `lixiang_auto`）
