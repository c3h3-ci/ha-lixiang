# 团队分析发现（2026-09-25）

> 来源：teammate `feature-gap`（截图分析）、`cmd-table`（实现路径）

---

## 一、已确认的修正（本轮已实施）

| 项 | 原值 | 修正为 | 证据 |
|---|---|---|---|
| **充电上限范围** | 50~100（步进 5）| **80~100（步进 1）** | App `minValue:80,maxValue:100,step:1` |
| **`xmode` 名称** | 离车模式 | **按时出发** | 路径 `MoveOffOnTime.Xmode` + 实测含 startTime/temp |
| **`xmode` 图标** | `mdi:car-off` | `mdi:clock-start` | — |
| **`ac_defrost` 名称** | 空调除霜 | **除雪除冰** | `XHttpOpenDeicingControl` 用 `dfstSw` |

**「除雪除冰」的关键证据（推翻 teammate 的"需实测"）：**
```
XHttpOpenDeicingControl.smali 的完整参数：
  cmdKey:  remoteVehACSmartControl
  cmdData: {acCtrlValue:"ON", acCtrlType:"dfstSw",
            acCountdownTimer:"15"/"18", acCtrlTemp:""}
→ App 的「除雪除冰」按钮 = dfstSw = 我们已有的 ac_defrost
→ 同一功能，只需改名
```

---

## 二、功能缺口（teammate 核实结论）

| 功能 | 结论 | 证据 |
|---|---|---|
| **增程器启动** | ❓ **待确认（我推翻了 teammate 的结论）** | 见下方"矛盾核实" |
| **按时出发** | ✅ 可实现 | 两个命令路径（见下）|
| **后视镜加热** | ❌ 不存在 | `Mirror+Heat` 组合 0 命中；smali 只有 `FoldStatus` |
| **直线召唤** | ❌ 不可行 | 走 `LXVehicleRNModule` 原生模块；`summon` 与 `sendVehicleJob` 同现 0 次；需手机钥匙+BLE |
| **里程统计** | 🔄 未找到 | 走 `LXVehicleInfoKeyMileage*` LiMesh 路径，具体 VSS 路径未找到 |

---

## 三、「按时出发」的两个命令（重要区分）

### A. `LXVehicleControlTypeAcAppointment` + `Fe()`
```javascript
sendVehicleJob(LXVehicleControlTypeAcAppointment, {cmdData: Fe()})
```
`Fe()` 返回 11 字段：
```javascript
{battWarmUp:false, flLevel:"1"-"7", frLevel:"1"-"7",
 rlLevel:"1"-"4", rrLevel:"1"-"4", startTime:"HH:MM",
 steerSet:"1"/"0", switchOn:true, temp:"26", vin, workingDay}
```
位置：@4534838（`appointmentAC` 页面）

### B. `LXVehicleControlTypeAppointmentAC` + `tt()`
```javascript
sendVehicleJob(LXVehicleControlTypeAppointmentAC, {
  cmdKey: 'MoveOffModify' 或 'MoveOffAdd',
  destParams: 'mob.metaJobService.MoveOffOnTimeData',
  data: tt(), timestamp: Date.now()
})
```
`tt()` 返回 16 字段（含 8 个座椅 + acSwitch + weekOfDayStates）：
```javascript
{id, subSwitch, startTime, isWorkDay, weekOfDayStates,
 acSwitch, temp, flLevel, frLevel, slLevel, smLevel, srLevel,
 tlLevel, tmLevel, trLevel, steerSet, battWarmUp}
```
位置：@4523002

### ★ 哪个对应状态信号？
```
状态信号: Vehicle.CarSettings.MoveOffOnTime.Xmode
实测内容: {mainSwitch, maxItemCount, moveOffDatas: {id: {
            acSwitch, subSwitch, startTime, temp, isWorkDay, weekOfDayStates,
            flLevel, frLevel, slLevel, smLevel, srLevel,
            tlLevel, tmLevel, trLevel, steerSet, battWarmUp}}}

→ 与 tt() 结构【完全一致】
→ 所以 B（AppointmentAC + tt()）才是车机设置里的「按时出发」
→ A（AcAppointment + Fe()）是 App 内的另一条路径（可能为临时预约）
```

---

## 四、其他重要发现

### 1. 集成代码有两个副本 ★ 注意别搞错
```
活跃仓库: /media/duola/devdata/AI-workspace/ha-lixiang/          （12 平台）
旧快照:   /media/duola/devdata/AI-workspace/lixiang-reverse/     （7 平台）
```
**所有改动必须改活跃仓库**（`ha-lixiang/`）。

### 2. 新版 App（8.25.3）新增 20 个 controlKey
```
宠物模式 ×3 / 洗车模式 ×2 / 场景模式 ×5 / 后视镜折叠 ×2 /
前备箱 ×4（含 W02B 车型变体）/ 具身迎宾 ×2 / FSDInit / Svm_
```
移除 1 个：`RemoteQuirSceneMode` → 修正为 `Quit`

反编译版本 8.16.1 vs 实机 8.25.3 —— **我们分析的是旧版**，
新版 APK 已提取到 `lixiang-reverse/apk_new/lixiang_8.25.3.apk`

### 3. 顶象 AOP 框架
```
com.lixiang.shield：23,805 个切点、1227 类含 redirect 字段
作为被包裹的业务：✅ 我们绕过桩读到全部原逻辑
作为框架自身：❌ 未覆盖 PatchFlowControl 热修通道、bsdiff、PatchesInfo

★ 若车控突然失效，优先怀疑【热修】而非我们的分析
```

---

## 四-补、★ 矛盾核实：「增程器启动」到底是什么

### teammate 的结论
```
"remote_engine_start 是 RN 键；HTTP 层实为 remoteVehAuth + cmdData:{}"
证据：VehicleControlModel.smali:359-371
       LXVehicleControlTypeRemoteStart → "remote_engine_start"
```

### 我的反驳（三条硬证据）

**① `remoteVehAuth` 已被证实是「虚拟钥匙授权」**
```
XHttpRemoteStartControl.smali 里的全部字符串：
  "remoteVehAuth"                        ← cmdKey
  "Vehicle.Cabin.RmtVirtualKeyAuthSts"   ← ★ 关联信号
  "remote Start:  "                      ← 调试日志

RmtVirtualKeyAuthSts = Remote Virtual Key Auth Status
                     = 远程虚拟钥匙授权状态

→ remoteVehAuth = 虚拟钥匙授权（App 首页的「授权驾驶」）
→ 与"增程器"无关
```

**② 旧版 JS bundle 里完全没有「增程器」**
```
搜索 unicode 转义：
  '增程器启动' (\u589e\u7a0b\u5668\u542f\u52a8): 0 次
  '增程器'     (\u589e\u7a0b\u5668): 0 次
  '启动增程器' (\u542f\u52a8\u589e\u7a0b\u5668): 0 次

含 Engine 的键: []（空）

→ 若该功能真的存在，JS 层应有文案
```

**③ smali 里也没有**
```
grep -rl "增程器" apk_decompile/ideal/smali_classes*/  → 无输出
```

### 可能的解释
```
① 截图里 OCR 的「增程器启动」可能是【OCR 误识】
   teammate 的 OCR 结果：'[2422] HEHN 增程器启动  按下'
   "HEHN" 是乱码 → 整个条目可能是误识

② 或者车控页的按钮是【服务端动态下发】的
   证据：存在 'vehicleEventTagUnityControlConfig' 事件标签
   → 按钮配置由服务端下发，本地 bundle 里没有文案

③ 或者该功能在新版 App（8.25.3）才加入
   但新版是 Hermes 字节码，未反编译
```

### 我的判断
```
❌ 不应把 remoteVehAuth 实现为「增程器启动」
✅ 我们当前的 button.授权驾驶 = remoteVehAuth 是【正确的】
⏳ 「增程器启动」的 HTTP cmdKey 【仍未确认】—— 需反编译新版或用 App 抓包
```

**教训**：teammate 的"推断"（RN 键→HTTP cmdKey）缺少直接证据链，
我差点据此实现错误的功能。**必须要求代码级证据，不接受推断。**

---

## 五、待办（按优先级）

### P0 — 已确认可实现
- [ ] ~~增程器启动~~ → **降级**（见"矛盾核实"，cmdKey 未确认）
- [ ] 「按时出发」按 B 方案实现（`AppointmentAC` + `tt()`）
- [ ] **按时出发**：先做「主开关 + 出发时间 + 温度」
  - 命令用 B 方案（`AppointmentAC` + `tt()` + `MoveOffModify`）
  - 状态读 `xmode` 信号的 `moveOffDatas[0]`
  - 建议实体：`switch.按时出发` + `time.出发时间` + `number.出发温度`

### P1 — 需继续挖
- [ ] 里程统计（`LXVehicleInfoKeyMileage*` 的 VSS 路径）
- [ ] 新版 20 个 controlKey（宠物模式/洗车模式/场景模式等）

### P2 — 不做（已确认不可行）
- ❌ 后视镜加热（不存在该功能）
- ❌ 直线召唤（需原生模块 + BLE + 手机钥匙）
